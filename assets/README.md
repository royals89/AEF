# assets

**Status:** Empty (Sprint 2 — structure only)

Static assets used by AEF's own documentation or tooling (diagram source
files, logos, etc.). Not part of the AEF runtime product surface — compare
with `examples/`, which holds runnable example projects.
