# .claude/templates

**Status:** Not yet populated (Sprint 1 — structure only)

Reusable prompt/config templates specific to the Claude Code runtime
integration (compare with the runtime-agnostic `templates/` at the repo
root). Populated starting Sprint 2+.
