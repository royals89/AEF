# config/schema

**Status:** Empty (Sprint 2 — structure only, no schemas yet)

Will hold schema definitions for every AEF configuration surface. See
[Configuration Specification](../../docs/specifications/CONFIGURATION_SPEC.md).
