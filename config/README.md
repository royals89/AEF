# config

**Status:** Structure only (Sprint 2)

The configuration model root: `schema/` for configuration schemas,
`profiles/` for named profile value sets. See
[Configuration Specification](../docs/specifications/CONFIGURATION_SPEC.md).
